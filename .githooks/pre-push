#!/usr/bin/env bash
set -euo pipefail

if [[ "${SKIP_LOCAL_TESTS:-0}" == "1" ]]; then
  echo "Skipping local tests because SKIP_LOCAL_TESTS=1"
  exit 0
fi

echo "Running deterministic extension tests before push..."
sbt test
